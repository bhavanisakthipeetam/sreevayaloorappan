---
import BaseLayout from "@layouts/BaseLayout.astro";
import { t } from "@i18n/translations";
import { getRelativeLocaleUrl } from "astro:i18n";
import { LOCALES } from "@i18n/locales";

export function getStaticPaths() {
  return LOCALES.map((lang) => ({ params: { lang } }));
}

const locale = Astro.currentLocale ?? "en";
const href   = (p: string) => getRelativeLocaleUrl(locale, p);

const offerings = [
  { num: "01", nameKey: "off_1_name", subKey: "off_1_sub", bodyKey: "off_1_body", tagsKey: "off_1_tags", detailKey: "off_1_detail" },
  { num: "02", nameKey: "off_2_name", subKey: "off_2_sub", bodyKey: "off_2_body", tagsKey: "off_2_tags", detailKey: "off_2_detail" },
  { num: "03", nameKey: "off_3_name", subKey: "off_3_sub", bodyKey: "off_3_body", tagsKey: "off_3_tags", detailKey: "off_3_detail" },
  { num: "04", nameKey: "off_4_name", subKey: "off_4_sub", bodyKey: "off_4_body", tagsKey: "off_4_tags", detailKey: "off_4_detail" },
] as const;

const steps = [
  { num: "1", titleKey: "step_1_title", bodyKey: "step_1_body" },
  { num: "2", titleKey: "step_2_title", bodyKey: "step_2_body" },
  { num: "3", titleKey: "step_3_title", bodyKey: "step_3_body" },
  { num: "4", titleKey: "step_4_title", bodyKey: "step_4_body" },
] as const;
---

<BaseLayout title={t("nav_offerings", locale)} description={t("offerings_desc", locale)}>

  <!-- ═══════════ PAGE HERO ═══════════ -->
  <section class="relative overflow-hidden text-center py-36" style="border-bottom:1px solid var(--border-gold); background:#fdf8f0;">
    <div class="absolute inset-0 pointer-events-none"
         style="background: radial-gradient(ellipse 60% 80% at 50% 50%, rgba(212,168,67,0.07) 0%, transparent 70%);" aria-hidden="true"></div>
    <div class="relative z-10 max-w-2xl mx-auto px-6">
      <span class="section-label">{t("offerings_label", locale)}</span>
      <h1 class="mb-4">{t("offerings_heading", locale)}</h1>
      <p class="text-text-sec text-lg">{t("offerings_desc", locale)}</p>
    </div>
  </section>

  <!-- ═══════════ INTRO GRID ═══════════ -->
  <section class="py-24" style="background:#f5ede0; border-bottom:1px solid var(--border-gold);">
    <div class="max-w-6xl mx-auto px-6">
      <div class="grid lg:grid-cols-[1fr_2fr] gap-16 items-center reveal">
        <!-- Left card -->
        <div class="text-center rounded-lg p-10" style="background:rgba(255,253,247,0.9); border:1px solid var(--border-gold); box-shadow:0 4px 20px rgba(0,0,0,0.06);">
          <div class="font-display text-5xl text-gold mb-4 opacity-80">ॐ</div>
          <h3 class="text-base mb-2">{t("pooram_card_title", locale)}</h3>
          <p class="text-sm text-text-mute mb-5">{t("pooram_card_desc", locale)}</p>
          <span class="divider-sm block mx-auto"></span>
          <p class="font-heading text-sage tracking-widest uppercase mt-4 mb-6" style="font-size:0.68rem;">{t("darshan_time", locale)}</p>
          <a href={href("/visit")} class="btn-outline w-full text-center block">{t("nav_visit", locale)}</a>
        </div>

        <!-- Right text -->
        <div>
          <span class="section-label">{t("rituals_label", locale)}</span>
          <h2 class="mb-4">{t("rituals_heading", locale)}</h2>
          <span class="divider-sm"></span>
          <p>{t("rituals_body1", locale)}</p>
          <p>{t("rituals_body2", locale)}</p>
          <p><strong class="text-ivory">{t("rituals_body3", locale)}</strong></p>
          <a href={href("/visit#contact")} class="btn-gold mt-4">{t("cta_book", locale)}</a>
        </div>
      </div>
    </div>
  </section>

  <!-- ═══════════ OFFERINGS DETAIL ═══════════ -->
  <section class="py-24" style="background:#fdf8f0;">
    <div class="max-w-5xl mx-auto px-6">
      <div class="text-center mb-12 reveal">
        <span class="section-label">{t("four_pariharams_label", locale)}</span>
        <h2>{t("four_pariharams_heading", locale)}</h2>
        <div class="divider-center"><span class="text-gold text-sm">✦</span></div>
      </div>

      <div class="space-y-5">
        {offerings.map(({ num, nameKey, subKey, bodyKey, tagsKey, detailKey }, i) => (
          <div
            class:list={["grid lg:grid-cols-[1fr_2fr] gap-8 items-start rounded-lg p-8 reveal transition-all duration-300", i === 1 && "reveal-d1", i === 2 && "reveal-d2", i === 3 && "reveal-d3"]}
            style="background:#fffdf7; border:1px solid var(--border-gold); box-shadow:0 2px 16px rgba(0,0,0,0.05);"
          >
            <!-- Left -->
            <div>
              <p class="font-display text-gold-dim tracking-[0.2em] text-xs mb-3">{t("offering_prefix", locale)} {num}</p>
              <h2 class="text-xl mb-2">{t(nameKey, locale)}</h2>
              <p class="font-heading text-sage tracking-widest uppercase mb-5" style="font-size:0.68rem;">{t(subKey, locale)}</p>
              <span class="divider-sm"></span>
              <p class="font-heading text-text-mute tracking-widest mt-4 rounded px-3 py-2 text-[0.72rem]"
                 style="background:rgba(212,168,67,0.08); border:1px solid var(--border-gold-light);">
                {t(tagsKey, locale)}
              </p>
            </div>
            <!-- Right -->
            <div>
              <p class="text-ivory-dim text-base leading-loose mb-3">{t(bodyKey, locale)}</p>
              <p class="text-sm text-text-mute">{t(detailKey, locale)}</p>
            </div>
          </div>
        ))}
      </div>
    </div>
  </section>

  <!-- ═══════════ HOW TO BOOK ═══════════ -->
  <section class="py-24" style="background:#f5ede0; border-top:1px solid var(--border-gold);">
    <div class="max-w-6xl mx-auto px-6">
      <div class="text-center mb-12 reveal">
        <span class="section-label">{t("booking_label", locale)}</span>
        <h2>{t("booking_heading", locale)}</h2>
        <div class="divider-center"><span class="text-gold text-sm">✦</span></div>
      </div>

      <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6 max-w-4xl mx-auto">
        {steps.map(({ num, titleKey, bodyKey }, i) => (
          <div class:list={["text-center p-6 reveal", i===1 && "reveal-d1", i===2 && "reveal-d2", i===3 && "reveal-d3"]}>
            <div class="w-12 h-12 rounded-full flex items-center justify-center mx-auto mb-5 font-display text-lg text-gold"
                 style="background:rgba(212,168,67,0.1); border:1px solid var(--border-gold);">
              {num}
            </div>
            <h3 class="text-sm mb-2">{t(titleKey, locale)}</h3>
            <p class="text-sm text-text-mute">{t(bodyKey, locale)}</p>
          </div>
        ))}
      </div>

      <div class="text-center mt-12">
        <a href={href("/visit#contact")} class="btn-gold">{t("cta_call", locale)}</a>
      </div>
    </div>
  </section>

</BaseLayout>
